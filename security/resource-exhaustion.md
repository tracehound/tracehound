# DoS & Resource Exhaustion Testing

If the security buffer fails, the entire model collapses.

Produce evidence:

- max memory
- backpressure
- queue bound
- regex worst-case
- parser worst-case
- event loop blocking
- fd leak

Artifact:

- latency / memory graph
