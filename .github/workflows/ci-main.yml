name: CI-Main

on:
  push:
    branches: [main]

concurrency:
  group: main
  cancel-in-progress: true

permissions:
  contents: read

env:
  PNPM_VERSION: 10.33.0

jobs:
  test_full:
    name: test_full (Node ${{ matrix.node-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: [20, 22]
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061 # v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm

      - run: pnpm fetch --frozen-lockfile
      - run: pnpm install --offline --frozen-lockfile
      - run: pnpm lint
      - name: Reject forbidden memory patterns in core
        run: |
          if grep -rn --include='*.ts' 'Buffer\.allocUnsafe' packages/core/src/; then
            echo "ERROR: Buffer.allocUnsafe is forbidden in packages/core/src/ — use Buffer.alloc"
            exit 1
          fi
      - run: pnpm build
      - run: node scripts/release/verify-publish-packages.mjs
        env:
          TRACEHOUND_RELEASE_LABEL: ci-main-${{ github.sha }}
      - run: pnpm test

  secrets_full:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0

      - uses: gitleaks/gitleaks-action@ff98106e4c7b2bc287b24eaf42907196329070c7 # v2.3.9
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE }}
          GITLEAKS_CONFIG: .gitleaks.toml
